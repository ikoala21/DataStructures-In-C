#include<stdio.h>
#include<conio.h>
#include<stdio.h>
int main()
{
    int i,j,n,key,fib[20],a[20],min;
    printf("Enter the value and key: ");
    scanf("%d %d",&n,&key);
    for(i=0;i<n;i++)
    {
        printf("enter the element: ");
        scanf("%d",&a[i]);
        printf("\n");
    }
    fib[0]=0;
    fib[1]=1;
    for(i=2;i<=20;i++)
        fib[i]=fib[i-1]+fib[i-2];
    i=0;
    j=n-1;
    n++;
    while(i<j)
    {
        min=n-fib[0];
        for(int k=1;k<n;k++)
        {
            if()
        }
    }
}
